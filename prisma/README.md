# Warum Prisma

Hier wird lediglich das Schema gepflegt und angewandt!

Das Hauptprogramm (deno auf opnsense) hat __KEIN PRISMA__
und macht die Queries mit reinem SQL.

Hier machen wir nur:

- npm run status (bzw. npx prisma migrate status)
- npx prisma migrate dev
