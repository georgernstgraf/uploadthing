<div class="leisure-card mx-auto text-center p-3 mw-600">
    <div class="mb-2">
        <div class="d-inline-flex align-center justify-center rounded-circle size-50 initial-avatar">
            <img src="/static/img/icons/check-lg.svg"
                 alt=""
                 width="28"
                 height="28">
        </div>
    </div>
    <h2 class="mb-3 fw-bold text-leisure-primary fs-3xl">Abgabe Erfolgreich!</h2>
    <div class="text-start p-3 rounded-4 bg-white-50">
        <div class="row g-2">
            <div class="col-12 pb-2 border-bottom border-light">
                <span class="d-block text-muted text-uppercase fw-bold fs-xs ls-normal">Dateiname</span>
                <span class="fw-bold text-break fs-xl">{{filename}}</span>
            </div>
            <div class="col-md-6 pb-1">
                <span class="d-block text-muted text-uppercase fw-bold fs-xs ls-normal">Größe</span>
                <span class="fs-lg">{{filesize}} Bytes</span>
            </div>
            <div class="col-md-6 pb-1">
                <span class="d-block text-muted text-uppercase fw-bold fs-xs ls-normal">Dauer</span>
                <span class="fs-lg">{{duration_seconds}} Sekunden</span>
            </div>
            <div class="col-md-6 pb-1">
                <span class="d-block text-muted text-uppercase fw-bold fs-xs ls-normal">IP</span>
                <span class="fs-lg">{{remote_ip}}</span>
            </div>
            <div class="col-12 pt-2 border-top border-light">
                <span class="d-block text-muted text-uppercase fw-bold fs-xs ls-normal">MD5-Summe</span>
                <span class="font-monospace text-break fs-md">{{md5sum}}</span>
            </div>
        </div>
    </div>
    <div class="mt-3">
        <button hx-get="/"
                hx-target="#main-content"
                hx-push-url="true"
                class="btn btn-primary px-4 py-2">Zurück zur Startseite</button>
    </div>
</div>