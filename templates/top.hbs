<!DOCTYPE html>
<html lang="en"
      data-bs-theme="dark">

<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, initial-scale=1.0">
    <title>PLF</title>
    <link rel="stylesheet"
          href="/static/bootstrap.css">
    <script src="/static/htmx.js"></script>
</head>

<body>
    <main class="container mt-4 p-4">
        <h1 class="mb-3 text-center">PLF - Router</h1>
        <a href="/whoami"
           class="d-block btn btn-lg mb-3 fw-bold d-flex flex-wrap align-items-baseline justify-content-around gap-4 {{#if remote_user}}btn-info text-dark{{else}}text-bg-danger btn-danger{{/if}}">
            {{#if remote_user}}
                <span>{{remote_user.name}} ({{remote_user.email}} / {{remote_user.klasse}})</span>
                <span class="badge text-bg-light text-uppercase">Change</span>
            {{else}}
                <span class="text-decoration-underline">Bitte Name bekanntgeben!</span>
            {{/if}}
        </a>
        <h5 class="text-center">Meine IP: {{remote_ip}}</h5>
        <nav class="d-flex justify-content-around gap-5 my-4">
            <a href="/"
               class="btn btn-outline-primary">Home</a>
            <a href="/forensic"
               class="btn btn-outline-light"
               style="opacity: 0;">Forensic</a>
            <a href="/upload"
               class="btn btn-outline-primary">Zipfile Upload</a>
        </nav>