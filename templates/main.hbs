<main id="main-wrapper"
      class="container mt-5 pt-3 p-2">
    {{> nav}}
    {{> userbadge}}
    <div class="text-center mb-2 opacity-50">
        <span class="small text-uppercase text-body-secondary">Meine IP: {{remote_ip}}</span>
    </div>
    <div id="main-content">{{{content}}}</div>
</main>
